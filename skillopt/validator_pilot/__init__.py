"""Opt-in, versioned experiments on verifier reliability and skill repair.

This package does not replace SkillOpt's default training loop or any previously
frozen cross-domain experiment. Model credentials are never experiment artifacts.
"""
