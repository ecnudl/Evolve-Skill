{
  "protocol_version": "artifact-scope-v2-pilot",
  "model": "gpt-5.5",
  "seed": 20260908,
  "workers": 6,
  "mechanisms": ["constraint_preservation"],
  "difficulty": "hard",
  "train_per_cell": 12,
  "dev_per_cell": 12,
  "validation_per_cell": 96,
  "test_per_cell": 24,
  "test_repeats": 2,
  "source_screen_range": [0.25, 0.95],
  "candidate_rounds": 2,
  "edit_budgets": [4, 2],
  "router_threshold": 0.75,
  "shuffled_seed": 1701,
  "gate": {
    "confidence": 0.95,
    "min_group_n": 16,
    "min_positive_gain": 0.01,
    "noninferiority_margin": 0.10,
    "max_conditional_harm": 0.15,
    "source_domains": ["coding"]
  },
  "notes": [
    "Successor experiment, not a replacement of the frozen first run.",
    "Artifact patches evaluated with a trusted bounded interpreter, not unrestricted code execution.",
    "Source train/dev learnability screen precedes validation and test access.",
    "If source screen fails, stop this run; source-only difficulty iteration requires a new run directory.",
    "One training seed; two generation repeats; conservative pilot risk tolerances, not a universal safety guarantee.",
    "All comparisons remain shared-draw offline policy replay with input-only routing."
  ]
}
